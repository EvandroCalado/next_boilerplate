import { Meta, StoryFn } from '@storybook/react';
import { {{pascalCase name}}, {{pascalCase name}}Props } from '.';

export default {
  title: 'Components/{{pascalCase name}}',
  component: {{pascalCase name}},
  args: {
    title: 'Next Template',
  },
  parameters: {
    layout: 'fullscreen',
  },
} as Meta;

export const Default: StoryFn<{{pascalCase name}}Props> = (args) => <{{pascalCase name}} {...args} />;
